# Tarea3 - Playwright + TypeScript

Contenido:
- tests/tarea3/ -> suites solicitadas (alerts, file upload, saucedemo data-driven)
- helpers/ -> logger, alertHelper, dataHelper, waitHelper
- pages/ -> POMs
- assets/ -> testfile.txt
- script tarea3 en package.json

Instalación:
1. npm install
2. npx playwright install

Ejecución:
- npm run tarea3
